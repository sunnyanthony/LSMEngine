FROM golang:1.21

WORKDIR /app
ENV CGO_ENABLED=0 GOCACHE=/tmp/go-build

COPY go.mod ./
RUN go mod download

COPY . .

# Run tests during build so the image only succeeds when tests pass.
RUN go test -v ./...

CMD ["go", "test", "-v", "./..."]
