namespace: lsm-example

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - configmap.yaml

configMapGenerator:
  - name: envoy-config
    files:
      - envoy.yaml

generatorOptions:
  disableNameSuffixHash: true
